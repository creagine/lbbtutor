<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/background_light</item>
        <item name="colorPrimaryDark">@android:color/background_light</item>
        <item name="colorAccent">@color/cappsTutor</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="AppThemeHome" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/cappsTutor</item>
        <item name="colorPrimaryDark">@color/cappsTutor</item>
        <item name="colorAccent">@color/cappsTutor</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="SplashScreenStyle" parent="Theme.AppCompat.NoActionBar">
    </style>

</resources>
